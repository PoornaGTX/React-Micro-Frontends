import('./bootstrap');

//Here, we use Bootstrap because the product app requires the Faker module to run independently (when only run product app).
//However, we configure it to be shareable in Webpack. That's why.
